# Vite fingerprints asset filenames (e.g. index-a1b2c3.js), so they can be
# cached forever — a new build produces new names. Replaces the equivalent
# header from vercel.json (Cloudflare does not read vercel.json).
/assets/*
  Cache-Control: public, max-age=31536000, immutable
